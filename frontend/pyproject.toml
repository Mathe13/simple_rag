[project]
name = "simple-rag-frontend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.28.2",
    "requests>=2.31.0"
]

[dependency-groups]
dev = [
]

[tool.pytest.ini_options]
asyncio_mode = "auto" 
testpaths = ["tests"]
